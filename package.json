{
  "name": "hono-starter",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "hono": "^4.7.2",
    "prettier": "^3.5.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
